import { components } from 'react-select';
import styled from 'styled-components';

const Menu = styled(components.Menu)`
  && {
    background-color: transparent;
    box-shadow: 1px 1px 10px 3px #00000037;
    border: none;
    overflow: hidden;
  }
`;

export default Menu;
